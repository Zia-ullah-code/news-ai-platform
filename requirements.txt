# Day 1 — ingestion + shared foundations. Later days append (duckdb, dbt-duckdb,
# kafka-python, langgraph, streamlit, ...) so each stage's deps stay traceable.
feedparser==6.0.11
requests==2.32.4
pydantic==2.11.7
pydantic-settings==2.10.1
tenacity==9.1.2
