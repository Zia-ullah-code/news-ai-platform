version: 2

sources:
  - name: raw
    description: Tables written by the scheduler's ingestion stage (not dbt).
    schema: main
    tables:
      - name: bronze_news
        description: >
          Append-only landing table for NewsMessage payloads consumed from the
          news.raw topic. Duplicates allowed by design; identity is article_id
          (see docs/design.md §1).
